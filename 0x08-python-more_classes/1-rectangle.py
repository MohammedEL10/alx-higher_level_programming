#!/usr/bin/python3
""" create Rectngle class"""
class Rectangle:
    """an empty class Reactangle"""
    pass
def width(self):
    width = int(width)
    raise:
        TypeError: width must be an integer
        if width < 0:
            raise:
                ValueError: width must be >= 0
                def height(self):
                    height = int(height)
                    raise:
                        TypeError: height must be an integer
                        if height < 0:
                            raise:
                                ValueError:height must be >= 0
                                def __init__(self, width=0, height=0):
