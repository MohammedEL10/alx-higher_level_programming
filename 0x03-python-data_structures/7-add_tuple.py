#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    new_tuple = ()
    tuple_i += (0, 0)
    tuple_j += (1, 1)
    new_tuple = tuple_i[0] + tuple_i[0],tuple_j[1] + tuple_j[1]
    return new_tuple
